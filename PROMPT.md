# Task Management Prompt
Please use README.md, DESIGN.md, the wireframes (TODO: FILENAME) and existing folder code infrastructure to build out a
simple task management utility using .NET Core, SQLLite with EFCore, and React.

## Core Workflow

## Frontend
Use React and MUI to create a bare-bones 3-columns with flexbox list items in App.tsx

The list items will contain a header text (`title`)

On task change, the UI will send a new POST request to the server.

## Set up a Docker config and container to ship this
## Backend

- Provide build options to:
    - Seed the database with a few sample tasks for generic tech company onboarding.
    - Ship an empty database schema for the UI to fill

- Provide unit tests